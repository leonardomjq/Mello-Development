# Mello Development

Landing page for a real estate development company. Plain HTML with Sass, compiled to `sass/main.css`.

To work on the styles, compile `sass/main.scss` with any Sass compiler, e.g.:

```sh
npx sass --watch sass/main.scss sass/main.css
```

Not maintained.
